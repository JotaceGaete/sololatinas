import MisRelatosClient from './components/MisRelatosClient';

export const metadata = {
  title: 'Mis Relatos | SoloLatinas',
  description: 'Gestiona tus relatos publicados y borradores.',
};

export default function MisRelatosPage() {
  return <MisRelatosClient />;
}
